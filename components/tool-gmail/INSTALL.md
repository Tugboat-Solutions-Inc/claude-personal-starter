## Gmail post-install

Authenticate Gmail through the official Anthropic MCP flow. From a fresh Claude session, run:

```
@claude_ai_Gmail authenticate
```

You'll be redirected to a Google sign-in page. Exact tool name may vary — check `mcp__claude_ai_Gmail__*` tools in your environment for the canonical authenticate command.
