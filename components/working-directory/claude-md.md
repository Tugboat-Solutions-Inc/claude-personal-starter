## Working directory

The user's working directory is `<<WORKING_DIR>>`. Treat that path as home base for everything they're actively working on.

When the user says "put this somewhere," "open my notes," or "save it" without specifying, default to that directory. Don't write outside it without asking first.
